{% for reference in history %}<a href="{{ path('files', { path: attribute(reference, 'path') }) }}" class="hover:underline cursor-pointer" async="true">{{attribute(reference, 'name')}}{{ not endsWith(attribute(reference, 'name'), directory_separator()) ? directory_separator()}}</a>{% endfor %}